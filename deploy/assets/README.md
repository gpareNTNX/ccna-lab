# Application icons

Optional branded icons can be placed here:

```text
app.ico   # Windows
app.icns  # macOS
```

The build scripts automatically use them when present. No vendor artwork is included by default.
